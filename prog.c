#include <stdio.h>
int main (){
int i, tab[10], j, max;
for ( i = 0; i < 10; i++)
{
  printf("give the numbers: ");
  scanf("%d", &j);
}
for ( i = 0; i < 10; i++)
{
  max = j;
  printf("the max is: %d", max);
}


    return 0;
}